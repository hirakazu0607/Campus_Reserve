<% content_for :title, "新規予約" %>

<div class="container mt-4">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card">
        <div class="card-header">
          <h2>新規予約</h2>
        </div>
        <div class="card-body">
          <%= render "form", reservation: @reservation %>
        </div>
      </div>

      <div class="mt-3">
        <%= link_to "予約一覧に戻る", reservations_path, class: "btn btn-secondary" %>
      </div>
    </div>
  </div>
</div>
